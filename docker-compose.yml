version: '3.8'

services:
  # PostgreSQL veritabanı / PostgreSQL database
  postgres:
    image: postgres:16-alpine # Hafif Alpine sürümü / Lightweight Alpine image
    container_name: my-postgres
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # --- TEST VERİTABANI (Sadece E2E Testleri İçin) ---
  postgres-test:
    image: postgres:16-alpine
    container_name: my-postgres-test
    environment:
      - POSTGRES_USER=test_user
      - POSTGRES_PASSWORD=test_password
      - POSTGRES_DB=test_db
    ports:
      - "5433:5432" # DİKKAT: Dış portu 5433 yapıyoruz, çünkü 5432 zaten postgres servisi tarafından kullanılıyor

  # MinIO (S3 uyumlu nesne deposu / S3-compatible object storage)
  minio:
    image: minio/minio
    container_name: nestjs-storage
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_ROOT_USER=${MINIO_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_PASSWORD}
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"

volumes:
  minio_data:
  postgres_data:
